import { BASE_PATH } from '@/constants/common';

export { default as BaseLayout } from '@/components/BaseLayout.vue';
export { api, RequestError, configStore } from '@/services';

export const CONSTANTS = {
  BASE_PATH
};
